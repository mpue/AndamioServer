Updates:

use andamio;

ALTER TABLE `NODE` ADD COLUMN `STUDYID` BIGINT(20) NULL DEFAULT NULL  AFTER `INHALT_ID` ;

ALTER TABLE `STUDYOBJECT` ADD COLUMN `INDICATION` BIGINT(20) NULL DEFAULT NULL  AFTER `type` ;

CREATE TABLE `INDICATIONOBJECT` (
  `INDICATION_ID` bigint(20) NOT NULL,
  `NAME` varchar(255) DEFAULT NULL,
  `DESCRIPTION` varchar(255) DEFAULT NULL,
  PRIMARY KEY (`INDICATION_ID`)
) ENGINE=MyISAM DEFAULT CHARSET=latin1

alter table STUDYOBJECT add RECRUITING bit(1);
alter table APPOINTMENT add COLOR text;

INSERT INTO `CONTENT` (`CONTENT_ID`, `DESCRIPTION`, `CONTENTNAME`, `PAGENAME`, `CONTENTSTRING`) 
VALUES ((Select max(INHALT_ID)+1 from `NODE`), 'Kolorektalkarzinom', 'Kolorektalkarzinom', 'Kolorektalkarzinom', '');

UPDATE `NODE` SET `TYPE`=1, `DISPLAYNAME`="Kolorektalkarzinom", `PLUGINID`=NULL, `PLUGINPARAMS`=NULL, 
`INHALT_ID`=(Select max(CONTENT_ID) from `CONTENT`) where NAME = "Kolorektalkarzinom";
